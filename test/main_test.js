import chai from "chai";

const expect = chai.expect;

// Test suite
describe("Mocha", () => {
    // Test spec (unit test)
    it("should run our tests using npm", () => {
        expect(true).to.be.ok;
    });
});