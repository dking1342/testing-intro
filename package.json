{
  "name": "cypress",
  "version": "1.0.0",
  "description": "cypress intro",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:cypress": "cypress",
    "cypress:open": "cypress open",
    "cypress:headless": "npx cypress run --headless"
  },
  "author": "David King",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^9.6.1"
  }
}
